require 'spec_helper'

describe users_controller do
	context "User render templates" do
		it "should render the new template when new called" do
		end
		it "should redirect to root when user creation success" do
		end
		it "should flash success message when user creation success" do
		end
		it "should render new template when user creation fails" do
		end
	end
end